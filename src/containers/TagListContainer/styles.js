import { StyleSheet } from 'react-native';
import { padding, backgroundColor } from '../../styles/base';

const TagListContainerStyles = StyleSheet.create({
	wrapper: {
		marginHorizontal: padding.screen,
	}
});

export default TagListContainerStyles;
